[project]
name = "portraits-core"
version = "0.1.0"
description = "Content-aware collage-to-portraits splitter core pipeline"
requires-python = ">=3.10"
dependencies = [
    "opencv-python-headless>=4.8,<5",
    "Pillow>=9.5",
    "numpy>=1.24",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
